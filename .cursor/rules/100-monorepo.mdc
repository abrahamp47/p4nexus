---
globs:
  - "p4nexus/**"
  - "p4nexus-web/**"
---

# P4Nexus build/test quick refs

- CLI (`p4nexus/`): `npm test`; `npm run test:integration`; `npx tsc --noEmit`.
- Web (`p4nexus-web/`): `npm test`; `npm run dev`; `npx tsc -b --noEmit`; `E2E=1 npx playwright test` (needs servers).
- `npm install` in `p4nexus/` runs `prepare` (tsc build) and `postinstall` (tree-sitter patches); needs `python3`, `make`, `g++`.
- LadybugDB locking tests may fail in containerized environments because of `/tmp` file locks (known issue, not a code bug).
